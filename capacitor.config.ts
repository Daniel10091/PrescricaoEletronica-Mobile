import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prescricao.eletronica',
  appName: 'prescricao-eletronica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
